---
title: 布隆过滤器
draft: true
tags: 
date:
---
# 是什么？

由布隆在1970年提出。布隆过滤器是一种**空间效率高的概率性数据结构**，用于判断一个元素是否在集合中。它具有以下特点：
- **核心优势**：用远少于传统数据结构（如哈希表）的空间存储数据集合的指纹
- **特性**：可能会有**假阳性（误判）**，但**不会有假阴性（漏判）**。不能判断一个元素是否一定存在，但是可以判断某个元素**一定不存在**。
- **应用场景**：网页去重、垃圾邮件过滤、数据库查重、缓存穿透预防等

# 实现原理

## 构成

布隆过滤器由以下两大核心组件构成
- **位数组（Bit Array）**：长度为 m 的二进制数组，初始全为 0
- **哈希函数组**：k 个独立的哈希函数，值域为 [0, m-1]

## 操作流程

- 向布隆过滤器添加元素时，会使用 **多个哈希函数** 对元素进行哈希运算，然后与数组长度取余，算出索引值，再降该位置的值改为1
- 布隆过滤器查询某个元素是否存在时，同添加一样，会使用 **多个哈希函数** 对元素进行哈希运算，然后与数组长度取余，计算出索引，判断是否每个索引位置的值都为1，如果存在0值，就表示布隆过滤器不存在元素。如果都为1，则**可能存在**（注意！！是可能存在布隆过滤器中）。

# 为什么是可能存在？

![[布隆过滤器-1751613248203.png]]

1. 向布隆过滤器中存入 137 与 153 这两个手机号。
2. 判断 186 手机号是否在布隆过滤器中存在时，多次哈希、取余计算的索引值都是1，但是这些值都不是186这个号码导致的，而是多个其他值设置的1。





# Redis 如何实现布隆过滤器

第三方的实现方式有：
- `Guava` 
- `Hutool`
- `Redisson`

此处以 `Redisson` 为例:

```java

        RBloomFilter<String> bloomFilter = redisson.getBloomFilter("myBloom");
 
        // 初始化布隆过滤器
        long expectedInsertions = 100_000_000L; // 预计插入元素数量
        double falseProbability = 0.001; // 期望的误报率
        bloomFilter.tryInit(expectedInsertions, falseProbability);
 
        // 添加元素
        bloomFilter.add("element1");
        bloomFilter.add("element2");
 
        // 查询元素是否存在
        boolean mightContainElement1 = bloomFilter.contains("element1");
        boolean mightContainNonExistent = bloomFilter.contains("nonexistentElement");
 
        System.out.println("element1 是否可能存在于集合中: " + mightContainElement1);
        System.out.println("不存在的元素是否肯定不在集合中: " + !mightContainNonExistent);
 
        // 获取布隆过滤器的信息
        System.out.println("布隆过滤器的大小（位数）: " + bloomFilter.getSize());
        System.out.println("布隆过滤器的误报率: " + bloomFilter.getFalseProbability());
        System.out.println("当前已插入元素数量: " + bloomFilter.count());
        System.out.println("布隆过滤器的状态: " + (bloomFilter.isExists() ? "存在" : "不存在"));

```
